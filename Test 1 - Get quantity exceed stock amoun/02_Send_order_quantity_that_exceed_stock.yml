info:
  name: 02_Send_order_quantity_that_exceed_stock
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/orders"
  body:
    type: json
    data: |-
      {
        "itemId": 2,
        "quantity": 2
      }
  auth:
    type: bearer
    token: "{{myBearerToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        if (data) {
          bru.setEnvVar("orderId", data.orderId); 
        }
  assertions:
    - expression: res.status
      operator: eq
      value: "400"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
