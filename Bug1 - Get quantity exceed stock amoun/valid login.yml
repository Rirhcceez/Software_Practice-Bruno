info:
  name: valid login
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5000/auth/login
  body:
    type: json
    data: |-
      {
        "username": "student",
        "password": "hungry"
      }
  auth:
    type: bearer
    token: ""

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        if (data && data.accessToken) {
          bru.setEnvVar("myBearerToken", data.accessToken);
          console.log("Token saved successfully!"); 
        }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
