info:
  name: Get all Menus
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5000/menu
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
