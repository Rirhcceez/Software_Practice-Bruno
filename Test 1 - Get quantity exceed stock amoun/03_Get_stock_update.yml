info:
  name: "03_Get_stock_update "
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/menu"
  auth:
    type: bearer
    token: "{{myBearerToken}}"

runtime:
  scripts:
    - type: after-response
      code: |-
        const data = res.getBody();
        if (data) {
          bru.setEnvVar("stockUpdate", data[1].stock); 
        }
  assertions:
    - expression: stockUpdate
      operator: eq
      value: "1"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
