info:
  name: Get a Order
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:5000/orders/ORD-cac389
  auth:
    type: bearer
    token: "{{myBearerToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
